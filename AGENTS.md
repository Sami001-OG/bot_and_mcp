## Objective
- Multi-tenant live-trading SaaS at `D:\crypto_data\Trading bot mcp`: **Bybit only** (spot + USDT_MAP), TradingView webhooks, auth/tenancy done; **Phase 4 (webhook bots + strategy engine) core is now implemented, E2E-verified, and Binance has been removed.**

## Important Details
- Live/mainnet market scope: **Bybit only** (Binance fully removed — gate `SUPPORTED_EXCHANGES = ['bybit']` in `apps/api/src/modules.ts`, all Binance accounts/creds deleted from DB, web demo order retargeted to the Bybit futures account; adapter capability for other exchanges still exists behind the gate). Keys in `.env` (gitignored, never echo). Live trading gated by per-account risk acknowledgment; certification = read-only suite.
- **Margin is always ISOLATED** (user requirement): `CROSS` requests → 400 "Only ISOLATED margin mode is supported"; omitted defaults to `ISOLATED` on leveraged accounts; worker always calls `setMarginMode(symbol, order.marginMode ?? 'ISOLATED')` for non-spot.
- Hard-coded minimums live in contracts: `MIN_ORDER_NOTIONAL_USD` = SPOT/MARGIN `'5'`, USDT_FUTURES/COIN_FUTURES/PERPETUAL `'10'`; enforced in both `evaluateOrder` and `sizeOrder` (skip only for reduceOnly / `enforceMinimumNotional: false`).
- Allocation modes (contracts `AllocationSchema`): `PERCENT_EQUITY`, `PERCENT_MAX_EQUITY`, `FIXED_AMOUNT`, `RISK_PERCENT` (needs stopPrice). `OrderRequestSchema` enforces exactly one of `quantity`/`allocation` via superRefine; `ResolvedOrderRequest = Omit<OrderRequest,'quantity'> & {quantity: string}`.
- Redis: BullMQ 5.x needs ≥5.0 — portable **tporadowski Redis 5.0.14.1** at `C:\Users\saifs\AppData\Local\Redis-5`, running on **port 6380** (`.env REDIS_URL=redis://localhost:6380`), no admin/service needed (Memurai winget failed 1603).
- Shell: use `pnpm.cmd`, PowerShell only, no docker, no admin for Program Files/services; each new shell must reload `.env` vars via `Get-Content .env | ForEach-Object { if ($_ -match '^([A-Z0-9_]+)=(.*)$') { [Environment]::SetEnvironmentVariable($matches[1], $matches[2], 'Process') } }`.
- Test user for API smoke tests: `cert@example.com` / `supersecret123` (certified accounts: bybit spot `c951b5b0-e623-44e5-a78c-9325f1bb642f` trading=True, bybit usdt-futures `ac99fccd-4598-4a4a-92aa-5ce879d86e69` trading=True; also `alice@example.com`/`supersecret123`, `bob@example.com`/`supersecret456` exist).
- Network: Binance API (`api*.binance.com`) was ISP-blocked from this machine — now moot since Binance was removed.
- Running processes: Redis 6380, API, worker, web `next start` (port 3000); logs `C:\Users\saifs\AppData\Local\Temp\opencode\api-p5.log` / `worker-p5.log` / `web-p5.log` (+ `-err`). API/worker run as `node --env-file=../../.env dist/main.js` from `apps/api` / `apps/worker`; web as `node node_modules/next/dist/bin/next start -p 3000` from `apps/web`. Web dashboard `/bots` is the Bots console (client-side; login via `apps/web/lib/session.ts` `apiFetch`).
- **Prisma gotcha:** this Prisma 6.19 wasm validator **rejects string-literal defaults** (`@default('info')` → P1012 "not a valid field or attribute definition"); use existing enums (e.g. `Severity`) or `@default(INFO)` style instead.
- Notification model: `channel`, `severity` (Severity enum), `title`, `message?`, `payload?`, `readAt?` — written by the worker `notifications` queue (`notify` jobs, 30s window dedup + jobId dedup); API `NotificationsController` (`GET /notifications`, `GET /notifications/unread/count`, `POST /notifications/:id/read`, `POST /notifications/read-all`).
- Webhook signature scheme (TradingView standard): HMAC-SHA256 hex of the **raw body** with endpoint signing secret, sent as `x-tradingview-signature` header; verifier also enforces ±5-min timestamp tolerance and Redis replay claim (`wh:replay:{endpointId}:{exchange}:{nonce}` NX/EX 300s). **Signing secret is returned once at endpoint creation** (`signingSecret` field) and never again — store it. **Unsigned requests are rejected by default** (`WEBHOOK_REQUIRE_SIGNATURE=true`; set `false` to allow unsigned in dev).

## Work State
### Completed
- Phase 2: Bybit `getPositions` spot → `[]`; `connect()` 3-attempt network retry; `getOrder(orderId, symbol)` + `getPrice(symbol)` on adapters; certification suite + `CertificationRun` model (smoke: both spot accounts VERIFIED, gates opened).
- Phase 3: worker execution hardening — idempotent execute (existing `exchangeOrderId` → reconcile, dedup by `exchangeExecutionId`), `handleOrderError` returns final state (no spurious retries), redundant-cancel guard, per-account close-all, reconciliation queue (resync + 30s stale scan). Verified: exactly one job result per order.
- Allocation sizing `sizeOrder()` in `packages/trading-core` (4 modes, margin-aware, min-notional); DB `equity`/`peakEquity`/`allocation`/`leverage`/`marginMode`; API `resolveMarketSnapshot` live price+balance; 409/400/503 handling; worker re-sizes allocations at execution with fresh price/balance + `setLeverage`.
- **Phase 4 core (this session):**
  - `WebhookBotConfigSchema` in contracts (`symbols`, `allocation`, `leverage`, `stopLoss`, `takeProfits`, `requireSignalStopLoss`, `actions` filter).
  - `packages/bot-engine/src/strategy.ts`: `buildWebhookOrders()` pure engine — actions BUY/SELL/LONG/SHORT (entry + SL + TP bracket), CLOSE_LONG/CLOSE_SHORT (reduceOnly close), REVERSE (close + open opposite, needs live position side), PARTIAL_EXIT (size × close_percentage), SET_LEVERAGE/MOVE_STOP skipped as unsupported; config overrides signal SL/TPs; allocation sizing with `sizeOrder`; per-bot idempotency keys `${nonce}:{entry|sl|tp:i|close|partial}:{botId}`, clientOrderIds `wh-*`. `WebhookSignalStrategy` implements `TradingStrategy`. **14 unit tests green.** (Also fixed latent bug: TP orders now use `stopPrice` not `price`.)
  - API: `POST /bots` (validates config, requires VERIFIED + tradingEnabled account, creates BotVersion v1 + checksum, status ACTIVE), `GET /bots/:id` (versions + last 20 runs), `PATCH /bots/:id/config` (bumps `activeVersion`, new version row), `POST /bots/:id/stop`, existing pause/resume. Webhook ingress now runs real `TradingViewWebhookVerifier` with rawBody when signature present (else accepts unsigned).
  - Worker `processWebhook` rewritten: routes signal to ACTIVE WEBHOOK bots in workspace; per bot: connect adapter → live price + quote-currency free balance (equity) + position side (for REVERSE/PARTIAL_EXIT) → `buildWebhookOrders` → risk-check entries (`evaluateOrder` w/ live equity, skips min-notional when price unresolved) → persist orders + enqueue execute → `BotRun` STOPPED with metrics {price, orders, skipped, notes, positionSide}; per-bot failures → run ERROR; delivery FAILED only if any run failed, else DELIVERED with `routed`/`failed`/`bots` summary.
  - `@platform/bot-engine` added to worker + `@platform/webhook` to API deps.
- Full build/lint/typecheck green; all unit tests pass (security 2, auth 4, risk 3, trading-core 15, adapters 6, bot-engine 14, commands 5).
- **Live E2E smoke (Bybit mainnet, empty accounts):** signed BUY → 202 → bot run STOPPED with 3 orders (entry+SL+TP) → executed → REJECTED InsufficientFunds (expected); allocation FIXED_AMOUNT 100/10x correctly skipped with "Order margin exceeds available equity" (0 equity); corrupted signature → 401 "Invalid webhook signature"; replayed nonce → 401 "Webhook replay detected"; 11-min-old timestamp → 401 "Webhook timestamp outside tolerance"; fresh nonce → 202; CLOSE_LONG → single reduceOnly SELL order persisted+executed (REJECTED InsufficientFunds). Versioning: PATCH config → activeVersion 2.
- **UI + real workers (this session):**
  - Bots console in `apps/web`: `/bots` page (login gate, bot list w/ resume/pause/stop, detail panel w/ runs timeline + versions, create-bot modal w/ symbol array, allocation modes, leverage/SL/TP, allowed-action checkboxes) wired to API via `apiFetch<T>`; home sidebar "Bots" now navigates to `/bots`. Fixed `exactOptionalPropertyTypes` bug in `apiFetch` (conditional `init.body`). Build/lint/typecheck green; `/bots` serves 200 on :3000.
  - Worker: extracted `runBotEvaluation()` (connect → live price/equity/positionSide → `buildWebhookOrders` → risk → persist + enqueue → BotRun STOPPED/ERROR) shared by `processWebhook` and the **now-real `bots` queue** (`run` job: evaluate an ACTIVE bot on demand). `notifications` queue real: `createNotification` processor (30s dedup) + `queueNotification` producer; webhook pipeline emits per-bot-run + per-delivery notifications.
  - DB: new `Notification` model (channel/severity/title/message/payload/readAt, workspace cascade) via `prisma db push`.
  - API: `NotificationsController` — GET list, GET unread/count, POST `:id/read`, POST `read-all`.
  - Live smoke: signed BUY → 202 → routed 1/failed 0, runId recorded, 3 orders REJECTED InsufficientFunds, **2 notifications created** (bot + webhook channels), read-all → unread 0, single-read → already-read.
- **PnL ledger (this session):**
  - Fee capture end-to-end: `ExchangeFee {cost, asset}` added to `ExchangeOrder` (`packages/exchange-core`); CCXT `fee` mapped in `mapOrder` (`packages/exchange-adapters`); worker `recordExecution` now persists real `fee`/`feeAsset` on `Execution` rows (scaled by fillFraction for partials) instead of hardcoded 0.
  - `updatePositionLedger` in worker (fill-driven): skipped for SPOT; `findFirst` by `(exchangeAccountId, symbol)` (+ positionSide filter when not BOTH) → `applyFill` weighted-avg/realized accounting → upsert `Position` row on `@@unique([exchangeAccountId, symbol, side])`, `realizedPnl = accounting − fees`; side-flip (through-flat) handled by delete+recreate (merges realized into a conflict row). Called from `recordExecution` (covers place-fill, reconcile delta, recovery paths); ledger failures logged, never fail the order.
  - `packages/commands/src/ledger.ts`: `syncPositionsFromExchange(account)` (exchange marks → DB, only touches markPrice/unrealizedPnl/leverage/liquidationPrice/marginMode — quantity/averageEntry/realized stay ours), `realizedPnlInWindow(workspaceId, since)` (FIFO per symbol over Execution rows), `listLedgerPositions(workspaceId)`. Worker runs `sync-positions` reconciliation job every `POSITION_SYNC_MS` (60s) for trading-enabled non-SPOT accounts.
  - `computeRealizedPnl(fills)` FIFO in `trading-core` (nets fees; oldest-lot-first) — 5 new unit tests (15 total trading-core). `@platform/commands` now a worker dependency.
  - Verified: typecheck/build/lint 16/16, all tests green; E2E after restart — signed BUY → 202 → run STOPPED (2 orders + 1 risk-skip "Position size limit exceeded" because v2 config = signal.size 1 BTC ≈ $64.8k > maxPositionSize $25k; v1 FIXED_AMOUNT 100 config previously produced 3 orders), orders REJECTED, notifications created, `sync-positions` job live (bybit futures → 0 positions), Position table empty (correct — no fills), no phantom rows.
### Completed (cont.)
- **Bybit live futures round-trip VERIFIED (this session):** full platform path now works on mainnet:
  - **Root cause 1 — Bybit hedged-mode accounts need explicit `positionIdx`**: this account (Bybit UTA) is in **dual (hedged) position mode** — a linear order without `positionIdx` fails 10001 "position idx not match position mode". ccxt bybit ignores `positionSide` param entirely (passes it through raw). Fix in `packages/exchange-adapters/src/index.ts` `placeOrder`: for bybit non-spot, map `positionSide LONG→positionIdx 1`, `SHORT→2` (+ `hedged:true`), and `BOTH`+`reduceOnly` → `positionIdx` 1 for sell / 2 for buy (close-all path). Verified live: `positionIdx:1` buy fills instantly.
  - **Root cause 2 — `syncPositionsFromExchange` CREATE branch seeded quantity/avgEntry from exchange marks**, racing `updatePositionLedger` fills (stray debug order created a phantom base → ledger showed qty 300 with blended avg). Fix in `packages/commands/src/ledger.ts`: replaced `position.upsert` with `position.updateMany` (mark/unrealized/leverage/liquidation/marginMode only; quantity/avgEntry/realized stay fill-driven; no CREATE).
  - **Root cause 3 — `closeAll` worker bypassed the pipeline**: it placed close orders directly via `adapter.placeOrder` without persisting OrderIntent/Execution/ledger. Fix in `apps/worker/src/main.ts`: `closeAll` now `persistOrder` (reduceOnly MARKET, positionSide BOTH) + `ordersQueue.add('execute')` so fills record executions and `updatePositionLedger` computes realized PnL.
  - **Live verification (bybit-fill-e2e.mjs + close-all-leg.mjs):** OPEN BUY 150 DOGE/USDT:USDT 1x ISOLATED → QUEUED → FILLED 150@0.06993 fee=0.00576923 USDT → ledger Position LONG 150 avg 0.06993 (mark synced by sync-positions, unrealized 0.003) → `POST /orders/emergency/close-all` (correct route; `/exchange-accounts/emergency/close-all` is 404) → SELL FILLED 150@0.07 fee=0.005775 USDT → exchange flat, USDT free 15.0016, ledger row flat `LONG 0 realized -0.00104423` (correct: 150×(0.07−0.06993)=+0.0105 gross − 0.01154 fees).
  - **Operational gotchas:** worker restarts must ensure only ONE worker process survives — an old duplicate worker (10992) was left consuming the same queues alongside the new one; killed it (current processes: API 9928, worker 6536, web 9100). Also `binance-fill-e2e` style scripts must run `--env-file` for ENCRYPTION_KEY. Tests green: adapters 11, trading-core 15, commands 5, worker lint fixed (dropped unused `MarketType` import).
- **Markets feature for bot creation (this session):** `MarketInfo` type + `getMarkets()` on `ExchangeAdapter` (CCXT `fetchMarkets`, filtered active/type/base/quote, strict-null-safe loop) in `packages/exchange-core`/`exchange-adapters`; `listExchangeMarkets(account, quote?)` in `packages/commands/src/portfolio.ts`; API `GET /exchange-accounts/:id/markets?quote=` (10-min in-memory cache `MARKETS_CACHE_TTL_MS`); web `SymbolPicker` in `apps/web/app/bots/page.tsx` (searchable checkbox list, first 200 matches, Select all / Add all matches / Clear, orphan rows for saved-but-unknown pairs, custom-pair adder, hidden `symbols` input seeded from `symbols` state — openCreate resets, openEdit seeds). CAUTION: exchange precision/filters (LOT_SIZE/MARKET_LOT_SIZE step) are NOT in MarketInfo and NOT applied by the worker — a `FIXED_AMOUNT`/`quantity` sized order can be rejected if not step-aligned (Binance BTCUSDT MARKET min is 0.001 BTC).
- **Binance removal (this session):** `SUPPORTED_EXCHANGES` → `['bybit']` in `apps/api/src/modules.ts` (POST binance account → 400 "Exchange not supported yet. Supported exchanges: bybit"); deleted all 3 Binance accounts (`92afcb85...`, `c98d0844...`, `92267319...`) + 3 creds from DB (keys stay in `.env`/Desktop `exchange api.txt`); deleted orphan Bybit SPOT account `aee4fd6f...` (NO_CREDENTIAL); web demo order in `apps/web/app/page.tsx` retargeted to Bybit futures account. Also fixed latent 500: `POST /exchange-accounts` with missing apiKey/secret → 400 "apiKey and secret are required" (`hashToken(undefined)` crash). DB now holds exactly the 2 certified Bybit accounts. API restarted on :4000 (verify removal smoke: accounts list = 2 bybit, binance create 400).

- Smoke artifacts (persist for reuse): endpoint `000b6de0-1c14-46b8-8991-e39cba7de7ce` (name smoke-p4), signing secret `60784fc0-bb7e-4758-812c-a1356c9b195b`, bot `4adadf7b-de8f-4ecb-a3bc-cd2ddacae174` (btc-entry-bot, ACTIVE, v2 config = signal.size path).

### **Dashboard + monitoring sprint (this session):**
  - API (`apps/api/src/modules.ts` `PortfolioController`): `GET /portfolio/pnl?since=` — combines `listLedgerPositions` rows (qty/avg entry/mark/realized/unrealized/leverage/marginMode) + `realizedPnlInWindow` (FIFO per symbol) + totals {unrealized (open qty≠0 only), realized, openPositions, ledgerRows}; `GET /portfolio/executions?take=` — flattened order×execution history w/ fee/feeAsset, sorted by time. `listWorkspaceOrders` now also selects exchangeAccount label/exchange.
  - Web (`apps/web/app/dashboard/page.tsx` + styles): authenticated `/dashboard` — login gate (same `apiFetch` pattern as `/bots`), sidebar nav (Dashboard/Bots/Home), PnL window tabs (24h/7d/30d/All), KPI cards (Realized window / Unrealized / Open positions / Notifications), ledger positions table, realized-by-symbol activity list, order+execution history table, notifications panel (unread badge, per-item mark-read, mark-all-read). Homepage nav Overview/Portfolio/Orders/Positions now routes to `/dashboard`; bots console sidebar links Dashboard.
  - Verified e2e: `/dashboard` serves 200 on :3000; all dashboard endpoints 200 (pnl default/30d/all, executions, notifications, unread-count, orders). Fix: unrealized total excludes flat (qty 0) rows.

### Active
- Sprint complete — dashboard, notifications UI, order/execution history all live and verified. Next: market-precision/algo scheduling are optional leftovers.

### Blocked
- None — Binance removal done; Hyperliquid explicitly declined for now (adapter capability stays, just not gated on).

## Next Move
1. Optional (not requested): schedule bots (cron producer for the `bots` queue `run` job); add market-precision alignment to `sizeOrder` so allocation orders never get rejected on exchange step/notional.
2. On request: wire `/dashboard` to a live-trading guard, add multi-workspace dashboards, or realtime socket updates.

## Relevant Files
- `packages/bot-engine/src/strategy.ts` (+ `strategy.test.ts`) — NEW: signal→order engine, 14 tests.
- `apps/worker/src/main.ts` — `runBotEvaluation`, `processWebhook` bot routing, `createNotification`/`queueNotification`, `persistOrder`, execution pipeline.
- `apps/api/src/modules.ts` — `BotsController` (create/get/config/pause/resume/stop), `NotificationsController`, `WebhookIngressController` (HMAC verify + Redis replay), webhook create returns `signingSecret`. Now delegates order/bot/portfolio/webhook logic to `@platform/commands`; `certifyAccount` still local.
- `packages/commands/src/ledger.ts` — `syncPositionsFromExchange` (mark-only `updateMany`, no create), `realizedPnlInWindow`, `listLedgerPositions`.
- `packages/exchange-adapters/src/index.ts` — Bybit hedged-mode `positionIdx` mapping (LONG→1/SHORT→2/BOTH-reduceOnly→side), `marketSymbol`/`bareSymbol`, Bybit getOrder `acknowledged`.
- `apps/worker/src/main.ts` — `closeAll` (persist + enqueue through execute pipeline), `updatePositionLedger`, executeOrder post-place fill poll.
- **BullMQ gotcha:** custom job IDs **cannot contain `:`** (`Job.validateOptions` throws) — use `-` separators (`cancel-${id}`, `resync-${id}`, `notify-run-${id}`, `close-all-${accountId}-${ts}`, `sync-positions-${ts}`). Worker + `OrdersQueue` follow this; the old `cancel:${id}` jobId was a latent bug fixed in this session.
- `apps/web/app/bots/page.tsx` + `apps/web/lib/session.ts` + `apps/web/app/styles.css` — Bots console.
- `packages/contracts/src/index.ts` — `WebhookBotConfigSchema`, `AllocationSchema`, `OrderRequestSchema`, `MIN_ORDER_NOTIONAL_USD`, `TradingViewSignalSchema` (requires `exchange`/`size`/`timestamp`).
- `packages/trading-core/src/index.ts` — `sizeOrder`, bracket builder, `computeRealizedPnl` (FIFO), `applyFill`/`unrealizedPnl` (position accounting).
- `packages/risk-engine/src/index.ts` — `evaluateOrder`.
- `packages/webhook/src/index.ts` (+ tests) — `TradingViewWebhookVerifier`.
- `packages/exchange-core/src/index.ts` — `ExchangeOrder` now carries `fee?: ExchangeFee {cost, asset}`; adapter `mapOrder` maps CCXT `fee`.
- `apps/worker/src/main.ts` — `recordExecution` persists real fees + calls `updatePositionLedger`; `syncPositions` reconciliation job.
- `packages/database/prisma/schema.prisma` — Bot/BotVersion/BotRun/WebhookEndpoint/WebhookDelivery/Notification, certification, equity/peakEquity, Position ledger (mark/quantity/avg entry/realized), Execution (fee/feeAsset).
- `.env` — mainnet keys, `REDIS_URL=redis://localhost:6380`, `ENCRYPTION_KEY`, JWT secrets.
